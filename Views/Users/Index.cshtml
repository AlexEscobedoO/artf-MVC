@model IEnumerable<artf_MVC.Models.User>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Tipouser)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nomuser)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Pauser)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sauser)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Mailuser)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Passuser)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Tipouser)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Nomuser)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Pauser)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sauser)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Mailuser)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Passuser)
            </td>
            <td>
                <a asp-action="Edit" class="btn btn-warning" asp-route-id="@item.Iduser">Edit</a> |
                    <a asp-action="Details" class="btn btn-primary" asp-route-id="@item.Iduser">Details</a> |
                <a asp-action="Delete" class="btn btn-danger"asp-route-id="@item.Iduser">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
